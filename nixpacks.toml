[phases.install]
cmds = [
  "apt-get update && apt-get install -y python3 python3-pip",
  "pip3 install --break-system-packages hdbscan numpy scikit-learn",
  "npm install --legacy-peer-deps"
]

[variables]
NODE_VERSION = "20"
PYTHON_VERSION = "3.12"

[start]
cmd = "node index.js"
